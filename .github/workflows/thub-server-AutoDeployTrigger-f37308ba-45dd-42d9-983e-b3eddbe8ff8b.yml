name: Trigger auto deployment for thub-server

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/thub-server-AutoDeployTrigger-f37308ba-45dd-42d9-983e-b3eddbe8ff8b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.THUBSERVER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.THUBSERVER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.THUBSERVER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.THUBSERVER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.THUBSERVER_REGISTRY_PASSWORD }}
          containerAppName: thub-server
          resourceGroup: gs_rg_0361
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


